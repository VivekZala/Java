package mypack2;

import java.lang.*;
import mypack1.values;

class values4_inherited_another_package extends values {

	// Here a is default so cannot be accessed outside package.
	// b is private so cannot be accessed outside a class.
	/* public void show() {
		System.out.println(a + b + c + d);
	}
	*/
}

